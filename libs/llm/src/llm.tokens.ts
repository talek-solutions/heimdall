/** Resolves to `ILlmConfig`. */
export const LLM_CONFIG = Symbol('LLM_CONFIG');

/** Resolves to the `ILLMProvider` selected by `PREFERRED_LLM_PROVIDER`. */
export const LLM_PROVIDER = Symbol('LLM_PROVIDER');
